{% extends 'base.html.twig' %}

{% block title %}
	{{author.username}}'s notes
{% endblock %}

{% block body %}
	<h1>{{ author.username }}</h1>
	{% for item in userNotes %}
		{{dump(item)}}
		<a href="{{path('app_note_show',{'slug': item.slug})}}">Show</a>
	{% endfor %}
{% endblock %}
